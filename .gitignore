# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.log
.env
venv/
.env/

# Node / Frontend
node_modules/
dist/
build/
.vite/
yarn.lock

# VS Code
.vscode/

# OS files
.DS_Store
Thumbs.db

# Trained model binaries (regenerate with train_xgboost_real.py + dqn_env.py)
models/saved_models/*.pth
models/saved_models/*.pkl
models/saved_models/*.json

# Keep benchmark text results (small, human-readable, useful to version)
!models/saved_models/xgboost_benchmark.txt
!models/saved_models/dqn_benchmark.json

# Simulation runtime outputs (generated when SUMO runs)
outputs/

# API key file — contains hardcoded credentials
simulation/live_ir_status.py

# Old deleted component
frontend/src/components/CustomPredictionPanel.jsx

# Data — keep CSVs, ignore network XML if you want (remove next 2 lines to track XML)
# data/network/*.xml
# data/network/*.osm